# Grow root partition to fill available disk space on first boot.
# For qcow2 VMs: virtual disk is pre-sized to 16G at build time.
# For raw images flashed to hardware: grows to fill the target disk.

[Partition]
Type=root
GrowFileSystem=yes
